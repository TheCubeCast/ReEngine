package com.thecubecast.ReEngine.Data;



public class ReadWrite {
	
	public static int[] LoadSettings() {
		//all the code that reads the file
	//	if (Settings file not created) {
			//create new settings file with universal settings that work for everyone 
	//	}
	//	else {
			//save each setting to a value in the array
	//	}
		return null; // returns the array containing each value in settings. Settings file format can be dynamically changed to add new values.
		
	}
	
}